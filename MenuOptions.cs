namespace EisenhowerCore
{
	public class MenuOptions
	{
		public enum EditingOptions
		{
			ChangeName, ChangeDate, ChangePriority, MarkOrUnmarkAsDone, Quit,
		}

		public enum Options
		{
			CreateNewItem, EditExistingItems, EditArchivedItems, RemoveItem, ArchiveItems, Quit,
		}
	}
}
